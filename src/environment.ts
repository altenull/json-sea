export const featureFlag = {
  debugMode: false,
  ogMetaPreview: true,
};

export const env = {
  localhost: 'localhost',
};

export const endPoint = {
  jsonLinkIO: 'https://jsonlink.io/api/extract',
};
